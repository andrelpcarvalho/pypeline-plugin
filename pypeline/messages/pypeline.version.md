# summary

Show the installed version of the pypeline plugin and check for updates.

# description

Displays the currently installed version of pypeline and checks npm for a newer release.
If a newer version is available, a warning is shown with the upgrade command.

# examples

- Show installed version and check for updates:

  <%= config.bin %> <%= command.id %>
